# CC-floor ciris-* wheel triple the evidence registry is derived against.
# Kept SEPARATE from matrices/current.yaml on purpose: current.yaml is the
# broad cohabitation matrix (owned by the floor-bump effort) and may lag or lead
# this triple mid-migration. The registry pins the exact floor whose live status
# `evidence/gen_cc_tests.py` recorded, so `git diff evidence/cc_tests.tsv` in CI
# is reproducible. Bumping the floor here (+ regenerating) is a deliberate
# registry update — see evidence/README.md.
#
# CC 1.0-rc2 floor — the CC 6.1.2.1.2 R9 cut: AggregationMetaV1 v2 → v3
# (CIRISVerify#191 wire / CIRISEdge#328 producer / CIRISPersist#435 admission).
# A pre-v3 aggregated tier now FAIL-CLOSES with aggregation_meta_multiplicity.
ciris-persist==16.1.0
ciris-verify==10.1.1
ciris-edge==11.1.1
